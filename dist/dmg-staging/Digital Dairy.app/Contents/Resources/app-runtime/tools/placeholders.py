from __future__ import annotations


def collect_vscode(start, end, dimensions, settings):
    """Placeholder for VS Code Copilot / chat stats.

    VS Code storage differs by extension and account. For now, import exported
    titles or stats through data/imports. This stub keeps the tool switch stable.
    """
    return []


def collect_chatgpt(start, end, dimensions, settings):
    """ChatGPT web titles are detected through browser history.

    For official exports, put title-level JSON/CSV in data/imports.
    """
    return []


def collect_doubao(start, end, dimensions, settings):
    """Doubao web titles are detected through browser history when present."""
    return []


def collect_deepseek(start, end, dimensions, settings):
    """DeepSeek web titles are detected through browser history when present."""
    return []
